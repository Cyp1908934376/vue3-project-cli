{
  "extends": "@vue/tsconfig/tsconfig.dom.json",
  "include": ["env.d.ts", "src/**/*", "src/**/*.vue"],
  "exclude": ["src/**/__tests__/*"],
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",

    // 1. 关键配置：设置 baseUrl 为根目录
    "baseUrl": ".",
    // 2. 关键配置：设置别名映射
    "paths": {
      "@/*": ["src/*"],
      "@config/*": ["config/*"], // <--- 加上这一行
      "@modules/*": ["modules/*"] // <--- 加上这一行
    }
  }
}
